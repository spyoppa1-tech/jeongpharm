# 블로그 키워드 검색 사이트 구축 계획서

## 1. 프로젝트 개요

**목적**
검색창에 키워드를 입력하면, healthdose 네이버 블로그에서 해당 주제와 관련된 포스팅(1~수개)을 자동으로 찾아 **링크 + 썸네일 + 요약**을 카드 형태로 보여주는 사이트를 만든다. **PC와 모바일 모두에서 최적화된 화면**으로 제공한다.

**배경**
- 블로그 포스팅이 누적될수록 방문자가 원하는 정보를 찾기 어려워짐
- 네이버 블로그 자체 검색 기능은 정확도가 낮고, 외부 유입 시 관련 글을 다시 안내하기 어려움
- 자체 검색 허브를 만들면 체류시간 증가, 내부 링크 강화(SEO), 브랜드 신뢰도 상승 효과 기대
- 건강 정보 특성상 모바일 검색 유입 비중이 높을 것으로 예상 → **모바일 경험이 전체 사용성을 좌우**

**핵심 사용 시나리오**
> 방문자가 (모바일에서) "크레아틴"을 검색 → 관련 포스팅 3개가 썸네일+요약과 함께 세로로 보기 좋게 노출 → 카드를 탭하면 원문(네이버 블로그)으로 이동

---

## 2. 핵심 기능 정의

| 기능 | 설명 |
|---|---|
| 키워드 검색창 | 자유 텍스트 입력, 실시간 또는 버튼 클릭 검색 (모바일에서는 터치 키보드 자동 포커스 고려) |
| 매칭 결과 | 제목/본문/태그 기준 관련도순 정렬, 1~N개 카드 출력 |
| 결과 카드 | 썸네일 이미지, 포스팅 제목, 2~3문장 요약, 원문 링크(새 창) |
| 반응형 레이아웃 | PC는 그리드(2~3열), 모바일은 1열 세로 스택으로 자동 전환 |
| 예외 처리 | 검색 결과 없을 때 인기글/최신글 노출 |
| 관리자 수동 최신화 | 새 글 발행 직후 다음 Cron을 기다리지 않고 즉시 반영하고 싶을 때, 관리자만 접근 가능한 "지금 최신화" 버튼/메뉴로 수집 파이프라인을 즉시 실행 |

---

## 3. 데이터 소스 및 수집 방안

- **네이버 블로그 RSS** (`https://rss.blog.naver.com/{블로그ID}.xml`)를 이용해 제목·링크·게시일·짧은 설명(description)을 1차 수집
- RSS의 description은 대부분 앞부분 일부만 발췌되어 불완전 → **본문 페이지를 추가로 fetch**해서 전체 텍스트 확보 필요
- **썸네일**: 각 포스팅 페이지의 `og:image` 메타태그를 스크래핑해서 추출 (네이버 블로그는 대부분 og:image 제공)
- 모바일에서는 이미지 로딩 속도가 체감 품질에 직결되므로, 썸네일은 **가벼운 압축본**을 사용 → 별도 리사이즈 인프라(서버 이미지 처리) 없이, og:image URL의 `?type=` 쿼리 파라미터를 `w300`으로 바꾸는 것만으로 네이버 CDN이 자체적으로 리사이즈된 경량 버전(테스트 결과 원본 101KB → w300 약 33KB)을 제공함을 확인
- 자사 블로그 콘텐츠이므로 저작권 문제는 없으나, 과도한 요청은 피하고 수집 주기를 적절히 조절(예: 하루 1회 배치)

---

## 4. 시스템 아키텍처 제안

기존에 사용 중인 스택(Cloudflare Pages + Claude Code)과 일관성을 맞춰 아래 구조를 제안합니다.

**공통 수집 파이프라인 (Option A/B 동일)**

```
[네이버 블로그 RSS] → [Cloudflare Worker (Cron)] → [본문 fetch + og:image 추출]
        → [Gemini API로 요약 생성] → [결과 JSON 생성]
```

수집까지는 두 방식이 동일하지만, 이후 **저장 방식과 사이트 반영 방식**은 Option A/B에 따라 달라집니다. (Worker가 KV에 데이터를 써도 정적 Pages 사이트가 이를 직접 읽을 수는 없으므로, 아래처럼 방식별로 다음 단계를 명확히 구분합니다.)

**두 가지 구현 방식**

- **Option A (완전 정적, 추천 — 우선 구현)**
  Worker가 생성한 JSON을 **Git 저장소에 커밋(또는 Cloudflare Pages Deploy API 호출)** → 그 커밋/호출이 Pages 빌드를 트리거 → 새 JSON이 포함된 상태로 정적 사이트가 재배포 → 프론트엔드에서 Fuse.js 같은 경량 라이브러리로 즉시 검색.
  → **저장소는 JSON 파일 하나**로 고정(KV 불필요). 서버 비용 없음, 구현이 단순함. 모바일 데이터 절약 측면에서도 유리(가벼운 정적 자원).

- **Option B (준동적)**
  Worker가 생성한 데이터를 **Cloudflare KV(또는 D1)**에 저장 → Pages에 **Pages Functions**를 붙여 요청 시점에 KV를 조회해 응답 → 신규 포스팅이 게시되는 즉시 자동 반영. 포스팅 빈도가 높아질 경우 이 방식으로 전환 고려.
  → **저장소는 KV/D1**로 고정(Functions 필요, "완전 정적"은 아님).

→ 초기에는 **Option A로 시작**하고, 운영하면서 필요 시 Option B로 확장하는 것을 권장합니다.

**신규 포스팅 자동 반영 + 수동 최신화**

- **자동(기본)**: Worker Cron이 하루 1회 실행되며, 새로 발행된 글의 postId만 골라 증분 처리(본문/썸네일 fetch → Gemini 요약 → JSON 갱신 → 재배포 트리거)합니다. 매번 전체 포스팅을 다시 처리하지 않아 비용과 시간을 절약합니다.
- **수동(관리자 전용, 결정됨)**: 사이트에 "지금 최신화" 버튼을 두고, 클릭 시 **Cloudflare Pages Function**(`functions/api/refresh.ts` 형태로 사이트와 함께 배포되는 경량 서버리스 함수, 별도 서버 불필요)을 호출합니다. 이 함수는 요청 헤더/쿼리로 전달된 **관리자 비밀키**(Cloudflare 환경변수로 저장, Gemini API 키처럼 브라우저에 노출되지 않음)를 검증한 뒤에만 동작합니다. 비밀키 검증에 실패하면 즉시 401 응답 — 방문자 누구나 누를 수 있게 열어두면 네이버 크롤링 요청과 Gemini API 호출 비용이 악의적으로 남용될 수 있기 때문입니다.
- 두 경로 모두 동일한 증분 처리 로직(현재 `scripts/sync.mjs`로 로컬 구현·검증 완료)을 공유하며, Cron은 이를 주기적으로, 버튼은 Pages Function을 거쳐 이를 즉시 호출하는 차이만 있습니다.
- 이 버튼/함수는 **3~4주차(프론트엔드 + Cloudflare 배포 단계)에서 실제로 붙입니다.** 현재(2주차)는 로컬에서 `node scripts/sync.mjs`를 직접 실행해 같은 로직을 이미 사용 중이며, 이후 이 로직을 Pages Function으로 그대로 옮기기만 하면 됩니다.

---

## 5. 요약 생성 파이프라인

1. RSS에서 신규 포스팅 감지
2. 본문 fetch → 불필요한 태그/이미지 텍스트 제거
3. Gemini API에 "이 포스팅을 2~3문장으로 핵심만 요약" 프롬프트 전달
4. 결과를 제목·링크·썸네일과 함께 JSON에 저장
5. 기존 발행 포스팅은 최초 1회 **일괄(batch) 처리**로 소급 색인화

---

## 6. UI/UX 구성 (모바일 최적화 포함)

**기본 원칙: 모바일 퍼스트로 설계 → PC는 확장**

- 상단: healthdose 브랜드 톤에 맞춘 심플한 검색창 1개 (모바일에서는 화면 상단 고정, 터치 영역 44px 이상 확보)
- 결과 영역:
  - **모바일(가로 ~480px 이하)**: 카드 1열 세로 스택, 썸네일은 카드 상단 또는 좌측 소형 배치, 요약은 2줄 이내로 축약 후 "더보기" 없이 바로 링크 이동
  - **태블릿(~768px)**: 카드 2열 그리드
  - **PC(~1024px 이상)**: 카드 3열 그리드
- 검색 전 초기 화면: 최근 포스팅 또는 인기 카테고리 노출 (모바일에서는 가로 스크롤 캐러셀 형태 고려)
- 폰트/터치 최적화: 본문 최소 16px(모바일 확대 방지), 버튼·카드 터치 영역 넉넉하게
- 이미지: `srcset` 또는 리사이즈된 썸네일로 모바일 데이터 사용량 절감, lazy loading 적용
- 성능: 모바일 저사양 환경 고려해 불필요한 애니메이션/무거운 스크립트 최소화

---

## 7. 개발 단계 로드맵 (총 4~5주 예상)

| 주차 | 작업 내용 |
|---|---|
| 1주차 | RSS 파싱 스크립트 작성, 전체 포스팅 목록 1차 수집 (Claude Code 활용) |
| 2주차 | 본문 fetch + og:image 썸네일 추출(모바일용 리사이즈본 포함), Gemini API 요약 자동화 |
| 3주차 | 프론트엔드 검색 UI 구현 — **모바일 레이아웃 우선 개발 후 PC 레이아웃 확장** (Fuse.js 기반 키워드 매칭) |
| 4주차 | Cloudflare Pages 배포, Worker Cron이 JSON 생성 후 Git 커밋/Deploy API로 자동 재배포 트리거 설정 |
| 5주차 | 실기기(모바일) 테스트 포함 검색 정확도·반응형 UI 점검, 실사용 피드백 반영 |

---

## 8. 예상 이슈 및 리스크

- **크롤링 제한**: 네이버 측 요청 빈도 제한 가능성 → 요청 간격 조절, 배치 주기 하루 1회 수준으로 설정
- **네이버 블로그 iframe 구조**: `blog.naver.com/{블로그ID}/{포스팅ID}` 접속 시 실제 본문/이미지는 PostView.naver iframe 내부에 렌더링되는 경우가 많아, 목록 URL을 그대로 fetch하면 본문 텍스트나 og:image를 못 가져올 수 있음 → iframe 내부의 실제 URL을 조합해 fetch하거나, iframe 없이 렌더링되는 모바일 버전(`m.blog.naver.com`)을 우선 시도하는 방식 검토 필요
- **RSS 범위 한계**: RSS는 최근 글 위주로만 제공될 수 있음 → 전체 과거 아카이브는 최초 1회 별도 수집 필요
- **요약 비용/무료 할당량**: 실제 구현 중 확인된 바로, Gemini 무료 티어는 모델별로 **하루 요청 한도**가 있음(예: `gemini-3.6-flash`는 하루 20건으로 매우 낮음) → 최초 전체 아카이브(240건) 요약처럼 대량 처리가 필요한 작업은 무료 한도가 넉넉한 **경량(lite) 모델**(`gemini-3.5-flash-lite` 등)을 사용하고, 신규 포스팅만 처리하는 평소 증분 동기화는 하루 처리량이 적어 무료 한도 안에서 충분히 운영 가능
- **검색 정확도**: 단순 키워드 매칭은 한글 형태소 변화(예: "크레아틴을", "크레아틴이")에 취약 → 필요 시 태그/동의어 사전 보완
- **모바일 성능**: 이미지 용량이 크면 모바일 로딩이 느려짐 → 썸네일 리사이즈/압축 필수, 첫 화면(above the fold)에는 최소한의 요소만 노출
- **네이버 CDN 핫링크(Referer) 차단**: 실제 구현 중 발견 — `blogthumb.pstatic.net` 썸네일은 요청에 제3자 도메인 Referer가 실려 있으면 403을 반환하고, 브라우저가 이를 `ERR_BLOCKED_BY_ORB`로 차단해 우리 사이트에서 썸네일이 아예 안 보이는 문제가 있었음 → `<img referrerpolicy="no-referrer">`로 Referer 전송 자체를 막아서 해결. 프론트엔드에서 og:image를 `<img>`로 직접 쓰는 모든 곳에 이 속성이 반드시 있어야 함

---

## 9. 향후 확장 아이디어

- 조회수·클릭률·체류시간 데이터를 반영한 정렬 기능과 결합(기존 구상 중인 벤치마킹 도구와 통합 가능)
- 카테고리 필터, 태그 클라우드 추가
- 검색어 자동완성 및 최근 인기 검색어 노출
- PWA(홈 화면 추가) 형태로 확장해 모바일 재방문율 향상
